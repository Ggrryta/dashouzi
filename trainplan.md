

## 能力模型映射

训练覆盖大厂后端面试/工作最看重的 **6 大能力维度**：

| 维度 | 对应项目 |
|---|---|
| **高并发 & 性能** | 秒杀系统、Feed 流 |
| **分布式系统** | 分布式任务调度、短链接服务 |
| **数据一致性** | 支付系统、库存扣减 |
| **IO 模型 & 网络** | IM 实时通讯 |
| **架构设计** | API 网关、微服务治理 |
| **稳定性 & 可观测性** | 全项目贯穿（日志、监控、告警、降级） |

---

## 训练方式

每个需求聚焦三件事：**搞懂技术点 → 写出代码跑通 → 面试能讲清楚**。直接在脑子里过一遍架构就开始写，不用搞需求评审、ADR 那些形式化流程。

**硬性规则：**

1. **所有项目必须用 Docker Compose 编排**，一键启动所有依赖
2. **配置地址必须用真实可路由 IP**，禁止 `127.0.0.1` / `0.0.0.0`
3. **集成测试 / E2E 测试必须走 Docker**，禁止本地直连跑
4. 高并发项目（秒杀、短链、IM、Feed）写完要有压测数据
5. **代码放 GitHub 公开仓库**，作为秋招作品集

---

## Level 1：单体夯实（预计 2 周）

### 需求 1：博客内容管理系统

> **业务背景：** 公司内部需要一个技术博客平台，用于沉淀团队知识。

#### PRD 摘要

| 模块 | 需求描述 |
|---|---|
| 用户系统 | 注册/登录（JWT）、角色权限（管理员/作者/读者） |
| 文章管理 | CRUD、草稿/发布状态机、Markdown 编辑器、标签分类 |
| 互动系统 | 评论（支持嵌套回复）、点赞/收藏 |
| 搜索 | 全文检索文章标题和内容 |
| 数据统计 | 文章阅读量、用户活跃度榜单（定时任务） |

#### 技术约束与挑战

- 数据库设计：用户-文章-评论-标签多对多关系，要求第三范式
- 状态机设计：`草稿 → 审核中 → 已发布 → 已下架`，不可逆跳转需校验
- 搜索：使用 PostgreSQL 全文索引或 ElasticSearch 集成
- 敏感词过滤：评论内容需通过 DFA 算法过滤
- 接口限流：登录、评论等接口需加令牌桶限流
- 单元测试覆盖率 > 70%
- **必须用 Docker Compose 一键启动（app + DB + 搜索）**

#### 你将学到的

- RESTful API 设计规范
- 数据库范式设计与索引优化
- 状态机模式的工程实践
- JWT 鉴权与 RBAC 权限模型
- 中间件模式（限流、日志、恢复）
- Docker Compose 编排

---

## Level 2：中间件 & 高并发（预计 3 周）

### 需求 2：商品秒杀系统

> **业务背景：** 电商平台双11大促，需要支持 10 万 QPS 的秒杀场景。

#### PRD 摘要

| 模块 | 需求描述 |
|---|---|
| 商品管理 | 秒杀商品上架、库存设置、秒杀时段配置 |
| 秒杀核心 | 用户抢购、库存扣减、订单生成 |
| 风控 | 单用户限购、黑名单、验证码 |
| 流量入口 | 秒杀倒计时、活动页面静态化 |

#### 硬性非功能需求（面试杀手锏）

- 核心接口 QPS ≥ 10,000（压测验证）
- **不超卖**（库存不能为负数）
- 接口响应时间 **P99 < 100ms**
- 支持水平扩展（无状态服务）

#### 技术挑战

1. **库存预热到 Redis**，秒杀期间不走 MySQL
2. **Redis Lua 脚本**保证库存扣减原子性
3. **消息队列异步下单**，削峰填谷
4. **前端 + 网关层限流**，令牌桶/漏桶算法
5. **缓存与 DB 最终一致性**，定时对账任务
6. **JMeter/Wrk 压测报告**，展示优化前后的 QPS 对比
7. **Docker 环境下跑压测**，不得本地直连

#### 你将学到的

- 高并发读写的分层架构（CDN → 网关 → 缓存 → MQ → DB）
- Redis 原子操作与 Lua 脚本
- 消息队列（Kafka/RabbitMQ）的削峰应用
- 压测工具使用与性能瓶颈定位
- CAP 理论在秒杀场景的实践

---

### 需求 3：短链接服务

> **业务背景：** 公司短信/推送渠道需要短链接来节省字符、做点击统计。

#### PRD 摘要

| 模块 | 需求描述 |
|---|---|
| 短链生成 | 长链 → 短链，支持自定义后缀、过期时间 |
| 重定向 | 301/302 跳转，记录点击日志 |
| 统计分析 | 点击量、UV/PV、地域分布、设备类型 |
| 安全 | 短链预览、黑名单域名检测、防钓鱼 |

#### 技术挑战

1. **分布式 ID 生成**（雪花算法 / 号段模式），全局唯一不重复
2. **Base62 编码**，将 ID 转为短码
3. **跳转性能**：Redis 缓存热点短链，不穿透 DB
4. **海量点击日志**：异步写入 ClickHouse/ELK
5. **布隆过滤器**：过滤不存在的短链请求，防止缓存穿透
6. **短链过期**：定时任务 + 延迟队列清理

#### 你将学到的

- 分布式 ID 生成方案对比（UUID / 雪花 / Leaf / 号段）
- Base62 编码原理
- 布隆过滤器的实际应用
- 跳转服务的高性能优化（零拷贝、连接池）

---

## Level 3：分布式系统（预计 4 周）

### 需求 4：实时 IM 聊天系统

> **业务背景：** 客服系统需要支持用户与客服实时聊天。

#### PRD 摘要

| 模块 | 需求描述 |
|---|---|
| 单聊 | 实时消息收发、已读/未读、正在输入 |
| 群聊 | 群组管理、群消息广播 |
| 离线消息 | 用户离线时的消息暂存与上线推送 |
| 历史记录 | 消息持久化、多端同步、关键词搜索 |

#### 技术挑战

1. **WebSocket 长连接管理**，心跳保活、断线重连
2. **连接路由**：用户 → 网关 → 指定 IM 节点，一致性哈希分配
3. **消息可靠性**：ACK 确认机制，消息不丢不重
4. **消息时序**：Lamport 时钟 / 向量时钟保证因果序
5. **离线消息拉取**：消息队列 + 推拉结合模型
6. **群消息扩散写**：大群用读扩散，小群用写扩散
7. **连接数压测**：同时维持 10,000+ 长连接

#### 你将学到的

- WebSocket 协议深入理解
- 长连接网关架构设计
- 消息可靠性投递（至少一次 / 精确一次）
- 推拉模型在 IM 场景的权衡
- 分布式时钟与消息排序

---

### 需求 5：分布式任务调度平台

> **业务背景：** 公司多个业务线有定时任务需求，需统一调度管理。

#### PRD 摘要

| 模块 | 需求描述 |
|---|---|
| 任务管理 | 创建/编辑/暂停/删除定时任务，Cron 表达式 |
| 调度引擎 | 时间轮触发，任务分发到 Worker 执行 |
| 执行器 | 支持 HTTP 调用、Shell 脚本、内嵌执行 |
| 容错 | 任务失败重试（指数退避）、超时告警、故障转移 |
| 监控 | 执行日志、耗时统计、成功率大盘 |

#### 技术挑战

1. **分布式调度**：多调度器节点，通过分布式锁选主（etcd/ZK）
2. **任务分片**：大任务分片到多 Worker 并行执行
3. **时间轮算法**：高效触发海量定时任务
4. **故障转移**：Worker 宕机后任务自动转移到健康节点
5. **幂等性保证**：防止任务重复执行
6. **DAG 工作流**：支持任务依赖编排

#### 你将学到的

- 分布式锁（Redis Redlock / etcd）
- 时间轮算法原理与实现
- 选举算法（Bully / Raft 基础）
- 幂等性设计方案（唯一键 / 状态机 / Token）

---

## Level 4：架构整合（预计 3 周）

### 需求 6：API 网关

> **业务背景：** 微服务数量激增，需要统一入口做认证、限流、路由、日志。

#### PRD 摘要

| 模块 | 需求描述 |
|---|---|
| 路由转发 | 根据 Path/Host/Header 路由到不同上游服务 |
| 认证鉴权 | JWT 校验、API Key 管理、IP 白名单 |
| 流量控制 | 接口级/用户级限流、熔断降级、灰度发布 |
| 协议转换 | HTTP → gRPC 转换、请求/响应改写 |
| 可观测性 | 全量请求日志、链路追踪（OpenTelemetry）、Metrics |

#### 技术挑战

1. **高性能代理**：基于 epoll/kqueue 的事件驱动模型
2. **限流算法**：实现固定窗口 / 滑动窗口 / 令牌桶 / 漏桶
3. **熔断器**：实现断路器模式（Closed → Open → Half-Open）
4. **动态配置**：路由规则热更新，不重启服务
5. **插件化架构**：认证/限流/日志以插件形式加载
6. **压测对比**：与 Nginx/Kong 做性能对比

#### 你将学到的

- 反向代理原理
- 网络编程（epoll / io_uring）
- 微服务治理核心组件
- 插件化架构设计
- 性能调优（零拷贝、连接池、内存池）

---

### 需求 7：支付系统（核心）

> **业务背景：** 秋招面试中**最能拉开差距**的项目。实现一套简化但完整的支付系统。

#### PRD 摘要

| 模块 | 需求描述 |
|---|---|
| 账户系统 | 用户账户、商户账户、账户冻结/解冻 |
| 支付流程 | 下单 → 支付 → 回调 → 入账 |
| 对账 | 定时拉取渠道账单，与内部流水 T+1 对账 |
| 退款 | 原路退回、部分退款、退款状态机 |

#### 技术挑战

1. **分布式事务**：支付扣款 + 订单更新，TCC 模式 / 本地消息表
2. **幂等性**：支付回调可能重复，需唯一流水号去重
3. **账户安全**：乐观锁扣款、账户余额不能为负
4. **对账差异处理**：差错处理、长款/短款挂账
5. **资金安全**：任何异常不得导致资金损失（最核心原则）
6. **状态机**：订单、支付、退款各一套严格状态机
7. **补偿机制**：超时未支付自动取消并退款

#### 你将学到的（面试高频考点）

- 分布式事务方案对比（2PC / TCC / Saga / 本地消息表）
- 最终一致性 vs 强一致性取舍
- 金融系统设计原则（资金安全第一）
- 对账系统设计
- 乐观锁 vs 悲观锁的应用场景

---

## Level 5：综合项目（预计 2 周）

### 需求 8：迷你 Twitter Feed 流系统

> **业务背景：** 将前面所有能力综合运用，实现一个社交 Feed 流。

#### PRD 摘要

| 模块 | 需求描述 |
|---|---|
| 用户关系 | 关注/取关、粉丝列表、关注列表 |
| Feed 发布 | 发帖（文字+图片）、删除 |
| Feed 流 | 关注流（Timeline）、推荐流（热度排序） |
| 互动 | 点赞、评论、转发、阅读计数 |

#### 技术挑战

1. **Timeline 推拉模型**：大 V 用读扩散，普通用户用写扩散，混合模式
2. **Feed 缓存**：每个用户 Timeline 缓存在 Redis List（限制长度 1000）
3. **热度算法**：实现简化版热度排序（Wilson 置信区间 / Hacker News 算法）
4. **计数服务**：点赞数/评论数高并发更新（Redis Hash + 定时刷 DB）
5. **图片处理**：上传 CDN + 缩略图 + 内容审核

---

## 训练执行路线图

```
Level 1: 博客系统(2周)
    ↓
Level 2: 秒杀系统 + 短链接服务(3周)
    ↓
Level 3: IM聊天 + 分布式调度(4周)
    ↓
Level 4: API网关 + 支付系统(3周)
    ↓
Level 5: Feed流系统(2周)
    ↓
复盘 + 简历包装(1周)
```

---

## 学习建议

- **前 3 个项目（Level 1+2）是基础**，完成后简历上已有足够亮点
- **支付系统（Level 4）是面试杀招**，至少做到 TCC 分布式事务
- 时间紧的话，IM 和调度平台可以二选一
- Feed 流是可选加分项，用于整合所有能力
- 每个项目写一篇技术博客，秋招面试时可以展开讲
